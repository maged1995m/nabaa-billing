{
  "name": "nabaa-billing",
  "version": "1.3.0",
  "description": "نبع - نظام فوترة المشتركين (أوفلاين) - يحفظ البيانات فعلياً على تخزين الجهاز عبر Capacitor Filesystem، ويدعم استيراد/تصدير Excel",
  "scripts": {
    "serve": "npx serve . -p 8080",
    "postinstall": "node scripts/copy-capacitor-web-assets.js",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  },
  "license": "UNLICENSED",
  "private": true
}

